- ShotSpot morning status: browser QA passed for the recent bug/PIN batch on the public app URL `https://msi.taila8c3ab.ts.net/`. Validated in browser: proof-of-arrival PIN gating, public URL rendering in tested flow, and empty-gallery-after-send regression.
- Remaining known issue after browser QA: authenticated create-session payload still leaks a localhost booking URL (`bookingUrl` using `http://localhost:3000/...`), so absolute URL generation is not fully cleaned up yet.
- Product direction captured from Naveen: customer should receive a unique per-customer per-session proof PIN and photographer should manually validate it; separate customer "start shooting" notification is not wanted for MVP.
- Implemented/validated overnight: public URL fixes for queue/gallery/checkout links, empty-gallery-after-send backend fix, and MVP proof-of-arrival PIN slice. PIN flow now generates a 6-digit PIN on queue join, includes it in queue-joined notifications, adds photographer verify endpoint, and blocks start until PIN verification.
- QA policy was formalized in Spotty workspace and ShotSpot QA skill: Tier 1 = automated/backend QA only, Tier 2 = browser QA required for browser-facing flow changes, Tier 3 = browser QA + screenshots for UI/visual changes.
- Morning architect recommendation for next parallel lanes after current bug batch: (1) booking payment finalization lane, (2) critical-path integration + dev gate hardening lane.
- Git/auth durable setup lesson: HTTPS pushes from this runtime failed due to terminal prompts disabled; Naveen set up SSH successfully afterward. Workspace repo push succeeded after switching to SSH. ShotSpot branch `wip/current-state-20260322` was rebased with conflicts resolved and pushed successfully; remote head ended at `57798b9`.
- Notification/deliverability status: SES email sending works after verifying identities, but notifications still feel unprofessional and land in spam. Likely next work should split into (a) remaining localhost/http leak cleanup and (b) notification template/content + sender/deliverability hardening (custom domain/DKIM/SPF/DMARC instead of Gmail sender).
