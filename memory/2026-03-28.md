# Memory Log — 2026-03-28

## ShotSpot Workstream 3: Signed Upload/Download Lifecycle — COMPLETED ✅

### What was delivered
- **Slice A (Frontend hardening):** `session-dashboard-client.tsx` updated with `Promise.allSettled`, image type filtering, UUID-based S3 keys, and error if `registeredCount === 0`.
- **Slice B (Download constraints):** `GET /api/gallery/:token` no longer leaks `download_token` unless `payload.paid === true`. `GET /api/download/:token` now returns 403 if `isDownloadTokenPaid()` returns false.
- **Slice C (Legacy endpoint removal):** `POST /api/orders/:id/photos` and `markOrderPhotosUploaded` fully removed from `parity-real.ts` and `repositories.ts`. `encodeInlinePhotoAsset` / `decodeInlinePhotoAsset` preserved for legacy data.
- **New method:** `isDownloadTokenPaid(token: string): Promise<boolean>` added to `DrizzleParityFlowRepository`.
- **Tests:** All passing with `--test-timeout=5000` enforced. 9/9 storage-photos tests, 3 new parity-real route tests.
- **Typecheck:** All 5 packages clean.
- **Commit:** `02fe1a5` pushed to `wip/current-state-20260322`.

### Key lessons from developer subagent run
- The first Developer subagent ran for 58+ minutes and got stuck in a loop on `repositories.paid-gallery.test.ts` (DB seeding issues, no timeout).
- Fix: always pass `--test-timeout=5000` to `npx tsx --test`. Added this to Developer brief and SOP.
- **Model used:** `codestral/codestral-2501` (primary working model for Developer role).

---

## SOP Updates Applied Today

### shotspot-coordinator/SKILL.md — Subagent Status Tracking SOP
Added rules:
1. **Rule 7:** For every subagent spawn, add a 1-minute cron job to check status until finished, then delete it.
2. **Rule 8:** During cron checks, if a subagent isn't making progress (looping on same error, stuck on a command), inspect `sessions_history` / `process(action=log)` and report the block reason to the user rather than waiting indefinitely.

---

## Dev Environment State (as of 7:38 PM PDT)

- **Web (Next.js):** Running on `localhost:3000` via `pnpm dev --filter=@shotspot/web`
- **API (Fastify):** Running on `localhost:4000`
- **Worker:** NOT running (pre-existing syntax error in `apps/worker/src/retention.mjs` — escaped backticks `\`` instead of template literal backticks — needs future fix)
- **Public URL:** `https://msi.taila8c3ab.ts.net/`
- **Tailscale serve config:**
  - `/` → `http://127.0.0.1:3000`
  - `/api` → `http://localhost:4000`
  - `/auth` → `http://localhost:4000/auth` ← **FIXED TODAY** (was stripping prefix, causing `/auth/google` 404)
- **Funnel:** Enabled (public internet access via Tailscale funnel)

### Google OAuth 404 Fix
- **Root cause:** Tailscale was configured as `/auth → http://localhost:4000` which stripped the `/auth` prefix. `GET /auth/google` arrived at Fastify as `GET /google` → 404.
- **Fix:** Reconfigured to `/auth → http://localhost:4000/auth` (prefix preserved). No code changes needed.
- **Google redirect URI:** `GOOGLE_OAUTH_REDIRECT_URI=https://msi.taila8c3ab.ts.net/auth/google/callback` (already correct in `.env.local`)

---

## Active Cron Jobs (cleaned up)
- `developer-status-poller` (9db0b08f) — REMOVED (original developer task finished)
- `oauth-fix-poller` (c8fc54a2) — REMOVED (oauth fix task finished)

---

## Next Priorities (per AGENTS.md ShotSpot order)
1. Worker/outbox notification processing (blocked by retention.mjs syntax error)
2. Paid Stripe purchase QA path
3. Automated QA/regression coverage hardening
4. Clean dev deploy gate preparation
5. Cloud deploy discussion (last)
