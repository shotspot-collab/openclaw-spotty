# 2026-03-31

- Updated ShotSpot workspace coordination state: task board and status now track API stability as completed and photographer login/Stripe re-test as the next active task.
- Updated `skills/shotspot-coordinator/SKILL.md` SOP so Architect completion must immediately trigger Developer handoff, coordination-file updates, and a workspace commit.
- Committed/pushed Spotty workspace coordination changes with commit `b3ccd25` (`chore: record API stability fix and architect-to-developer handoff SOP`).
- Delegated API stability fix to Developer; implemented graceful shutdown in `apps/api/src/server.ts` plus launch-mode docs in `apps/api/README.md` and `docs/local-dev-runtime.md`.
- Reconfirmed stable detached API startup path should be `pnpm --dir apps/api start:tsx`; stale listeners on 4000 can still cause `EADDRINUSE` until explicitly cleared.
- Restored Tailscale serve/funnel mappings to:
  - `/` -> `http://127.0.0.1:3000`
  - `/api` -> `http://localhost:4000`
  - `/auth` -> `http://localhost:4000/auth`
- Diagnosed Google OAuth issue with Architect, then delegated fix to Developer.
- Developer updated `apps/api/package.json` dev script to explicitly load repo-root `.env.local` via `dotenv-cli`, which made local `/auth/google` return a 302 and generate callback `https://msi.taila8c3ab.ts.net/auth/google/callback`.
- End-of-day state: local web is up and local Google OAuth route can work, but the public Tailscale path `https://msi.taila8c3ab.ts.net/auth/google` still intermittently returns HTTP 502, so live Google sign-in remains unresolved.
- Process lesson: earlier user-facing status messages overstated success before re-checking the public path; future updates should distinguish local verification from public/Tailscale verification explicitly.
